@model Turno
@{
    ViewData["Title"] = "Appointment Confirmed - Glow & Style";
}
@section DashboardServices {
    <link rel="stylesheet" href="~/css/books-success.css" asp-append-version="true" />
}

<div class="turnos-wrapper">
    <div class="success-card">
        <div class="success-icon">
            <svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" viewBox="0 0 24 24"
                 fill="none" stroke="#d4af37" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14" />
                <polyline points="22 4 12 14.01 9 11.01" />
            </svg>
        </div>
        <h1>Appointment confirmed!</h1>
        <p class="success-subtitle">Your appointment has been booked successfully.</p>

        <div class="confirm-card">
            <div class="confirm-details">
                <div class="confirm-row">
                    <span class="confirm-label">Service</span>
                    <span class="confirm-value">@Model.TurnoServicios?.FirstOrDefault()?.Servicio?.Nombre</span>
                </div>
                <div class="confirm-row">
                    <span class="confirm-label">Professional</span>
                    <span class="confirm-value">@Model.Profesional?.Usuario?.Nombre @Model.Profesional?.Usuario?.Apellido</span>
                </div>
                <div class="confirm-row">
                    <span class="confirm-label">Date</span>
                    <span class="confirm-value">@Model.FechaHora.ToString("dddd, MMMM dd, yyyy")</span>
                </div>
                <div class="confirm-row">
                    <span class="confirm-label">Time</span>
                    <span class="confirm-value">@Model.FechaHora.ToString("HH:mm")</span>
                </div>
                <div class="confirm-row">
                    <span class="confirm-label">Code</span>
                    <span class="confirm-value">#@Model.Id_Turno</span>
                </div>
            </div>
        </div>

        <div class="success-actions">
            <a asp-action="MyAppointments" class="btn btn-primary">My appointments</a>
            <a asp-controller="Home" asp-action="Index" class="btn btn-secondary">Back to home</a>
        </div>
    </div>
</div>
